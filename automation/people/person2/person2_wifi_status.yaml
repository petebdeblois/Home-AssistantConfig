alias: "Julie WiFi status"
initial_state: "on"
trigger:
  - platform: state
    entity_id: sensor.pixel_xl_wifi_connection
action:
  - choose:
      - conditions:
          - condition: template
            value_template: "{{ 'Cogs-n-Gears' in states('sensor.pixel_xl_wifi_connection') }}"
        sequence:
          - service: mqtt.publish
            data:
              topic: location/Julie_wifi
              payload: "home"
              retain: true
    default:
      - service: mqtt.publish
        data:
          topic: location/Julie_wifi
          payload: "not_home"
          retain: true
